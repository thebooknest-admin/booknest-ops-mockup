/** Phase 1 returns-domain boundary. Procedure implementations remain unchanged. */
import { legacyAppRouter } from "../_legacy/legacy-app-router";

export const returnsRouter = legacyAppRouter.returns;