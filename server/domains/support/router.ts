/** Internal support route boundary retained for a later dedicated extraction. */
import { legacyAppRouter } from "../_legacy/legacy-app-router";

export const supportRouter = legacyAppRouter.support;